pipeline {
    agent any
    stages {
        stage ('ansible code') {
            steps {
                // get the ansible code from github
                git credentialsId: 'gitkey', url: 'git@github.com:gopal1409/mass-ansibleplay25.git'
            }
        }
    }
}
